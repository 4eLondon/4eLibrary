# -o-o-o-o-o- [ Ruby comments ] -o-o-o-o-o-

# Single line comments in ruby begin with #.

=begin
  If you wish to create a multiline comment it must be in this very order.
  We begin with =begin to open our comment and close with =end.
  Any content between our =begin and =end will be ignored/commented out.
=end

puts "We can write single line comments in ruby with #"
puts "Eg. # This is a comment in ruby. Comments begin with #"

puts "For multiline commens open the comment with =begin and close with =end"
puts "Eg. =start
This is a multiline comment.
This comment spans several lines.
Open the comment with =begin
Close the comment with =end"

