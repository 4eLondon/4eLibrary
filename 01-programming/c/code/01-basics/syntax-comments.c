// -o-o-o-o-o- Comments -o-o-o-o-

// Each of these lines are a single-line comment.
// Note how each must start with the double forward-slash

/*
 * This is a multiline comment.
 * Note how we start with a forward-slash
 * and each line after is followed by an asterisk
 * before ending with another forward-slash
 */

#include <stdio.h>

int main() {
  printf("Make comments in C using //");
  printf("Make multiline comments in C using /**/");
  return 0;
}
